name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main  # Un branch name 'master'-nu irundha 'master'-nu mathiko

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Install and Build 🔧
        run: |
          npm install
          npm run build

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # Vite generate panra folder 'dist' dhaa
          branch: gh-pages # Build files ellam indha branch-ku pogum
          
